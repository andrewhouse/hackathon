= simple_form_for([@folder, @item]) do |f|
  = f.error_notification

  .form-inputs
    = f.hidden_field :folder_id, value: @folder.id
    = f.input :title
    = f.input :record

  .form-actions
    = f.button :submit
